function findMaxNumber(arr) {
  // Your implementation

  console.log("Arr: ", arr);
  let maxNum = null;

  if(!arr.length) return maxNum;

//   for(let i = 0; i )

}

//For the purpose of user debugging.
findMaxNumber([1, 2, 3, 4, 5]);
